import React from 'react';

const Rate = React.createClass({
    render() {
        return (
            <div className="rate">
                Rate
            </div>
        )
    }
})

export default Rate;
