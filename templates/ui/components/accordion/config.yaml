# Component documentation and configuration
status: 'production'
classBase: 'c-accordion'
displayName: 'Accordion'
flavours:
  - base
dependencies:
  - core

# Generates the `Data` table
schema:
  title:
    type: string
    description: ""

# Generates the `Usage` table
usage:
  - class: .c-accordion
    required: True
    elements:
      - div
    outcome: Defines the Accordion component
    comments: Wrapping element, adds margin at bottom
