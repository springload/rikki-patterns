# Component documentation and configuration
status: 'production'
classBase: 'c-callout'
displayName: 'Callout'
flavours:
  - base
dependencies:
  - core

# Generates the `Data` table
schema:
  title:
    type: string
    description: ""

# Generates the `Usage` table
usage:
  - class: .c-callout
    required: True
    elements:
      - div
    outcome: Defines the Callout component
    comments: Wrapping element, adds margin at bottom
