var React = require('react');


module.exports = React.createClass({
    render() {
        return (
            <div className="c-card">React-driven component (if applicable)</div>
        )
    }
})
